[package]
name = "rust_sdl_bridge"
version = "0.1.0"
edition = "2024"

[dependencies]
sdl2 = { version = "0.37.0", features = ["image", "mixer"] } # image and mixer features are optional

[lib]
crate-type = ["staticlib"]  # or "cdylib" for a shared library